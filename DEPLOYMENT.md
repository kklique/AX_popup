# 공개 배포 안내

이 저장소의 화면은 GitHub Pages를 통해 공개할 수 있습니다.

## 처음 한 번 설정하기

1. GitHub 저장소의 **Settings → Pages**로 이동합니다.
2. **Build and deployment**의 Source에서 **GitHub Actions**를 선택합니다.
3. `main` 브랜치에 변경 사항을 올리면 자동 공개 작업이 실행됩니다.
4. **Actions** 메뉴에서 완료 표시를 확인합니다.

## 알아둘 점

GitHub Pages는 화면 파일을 공개하는 정적 호스팅입니다. 따라서 통합검색의 초기 화면, 대시보드, 월별 통계는 공개할 수 있지만 SQLite 데이터베이스와 매일 자동 수집 기능은 GitHub Pages에서 실행되지 않습니다.

실시간 수집까지 운영하려면 FastAPI 서버와 데이터베이스를 별도의 상시 서버에 배포해야 합니다. 공개 화면은 그 서버의 API 주소를 환경 설정으로 연결하는 방식이 적합합니다.
